# spring boot server
server.port=8099
server.servlet.context-path=/

# trino proxy.
trino.proxy.port=18080
trino.proxy.authentication=true
trino.proxy.tls.enabled=true
trino.proxy.tls.keystorePath=/home/opc/keystore.jks
trino.proxy.tls.keystorePass=changeit
trino.proxy.tls.trustStorePath=/home/opc/.keystore
trino.proxy.tls.trustStorePass=changeit

# jdbc
jdbc.driverClassName=com.mysql.jdbc.Driver
jdbc.url=jdbc:mysql://localhost:3306/trino_proxy?useSSL=false
jdbc.user=trino
jdbc.pass=Trino123!

# hibernate
hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
hibernate.show_sql=true
hibernate.globally_quoted_identifiers=true


